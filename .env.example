# Example environment variables for local development
# Never commit real secrets.

NODE_ENV=development
BILLING_ADAPTER_RESOLUTION_MODE=fallback
BILLING_INGEST_MODE=deterministic
FICECAL_API_BASE=http://localhost:3000
FICECAL_MCP_BASE=http://localhost:4000
FICECAL_CREDENTIALS_BACKEND=local-dev
FICECAL_SECRET_RESOLVER_ENDPOINT=http://localhost:4100/resolve
FICECAL_OPENOPS_CREDENTIAL_REF=cred://openops/local/readonly
FICECAL_AWS_CREDENTIAL_REF=cred://aws/local/readonly
FICECAL_AZURE_CREDENTIAL_REF=cred://azure/local/readonly
FICECAL_GCP_CREDENTIAL_REF=cred://gcp/local/readonly
