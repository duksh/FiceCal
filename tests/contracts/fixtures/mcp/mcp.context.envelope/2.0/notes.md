# mcp.context.envelope fixture notes (2.0)

- `request.valid.json` provides a canonical MCP v2 context + tool input envelope.
- `request.invalid.json` captures required-field and type violations for context validation.
- Envelope contract is shared across MCP v2 tools.
