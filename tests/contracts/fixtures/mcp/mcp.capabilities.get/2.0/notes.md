# mcp.capabilities.get fixture notes (2.0)

- `response.expected.json` is the canonical capabilities handshake baseline.
- Tool namespace and compatibility metadata must be kept in sync with MCP tool exports and parity fixtures.
