# Legacy Alias Parity Fixture Notes (1.0)

- Seed parity rows for `F2-TASK-033` are defined in `parity.rows.json`.
- Each row maps a `finops.*` legacy alias to the canonical `billing.*` tool.
- This fixture set is baseline-only; execution automation is completed in downstream parity test implementation.
