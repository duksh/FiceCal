# Fixture Notes: community.sample-finops-adapter@0.1.0

- `input.valid.json` verifies deterministic normalization and coverage.
- `input.invalid.json` verifies non-numeric `cost` handling.
- `output.expected.json` is the golden output for valid fixture parity.
